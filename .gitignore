# Dados brutos pesados (255 MB — acima do limite de 100 MB por arquivo do GitHub)
adhdata.csv
*.zip

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
env/
*.egg-info/

# Ambiente / segredos (por precaução — hoje o projeto não usa nenhum)
.env
.env.*
!.env.example

# Sistema / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Saída derivada dos scripts: regerável a qualquer momento rodando
# inventario_hbn.py, fenotipo_hbn.py, qc_relatorio.py e figuras_limpeza.py.
# Versionar relatório é versionar uma foto de uma execução, que envelhece
# sem avisar e passa a contradizer o código que a produziu.
relatorios/

# Capturas de tela de verificação no navegador
.playwright-mcp/
